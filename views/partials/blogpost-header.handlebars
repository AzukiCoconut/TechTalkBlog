<div class="card m-2">
    <a href="/blogpost/user/{{id}}">
    <header class="d-flex justify-content-between align-items-center p-2 bg-body-secondary">
        <h3>{{title}}</h3>
        <p>{{format_date created_on}}</p>
    </header>
    </a>
</div>